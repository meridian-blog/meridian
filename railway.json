{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "deno run --allow-net --allow-read --allow-env --allow-write=./uploads db/migrate.ts && deno run --allow-net --allow-read --allow-env --allow-write=./uploads backend/main.ts",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
